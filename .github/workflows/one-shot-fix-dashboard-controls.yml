name: One-shot dashboard interaction fix

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/one-shot-fix-dashboard-controls.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Patch dashboard interactions
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib import Path

          def replace_once(path, old, new, label):
              p = Path(path)
              text = p.read_text()
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f'{label}: expected 1 match in {path}, found {count}')
              p.write_text(text.replace(old, new, 1))
              print(f'patched: {label}')

          # Keep the newer dashboard-back fix already on main. Complete the same
          # root-deck logic for Messages and for switching an exhausted deck to Events.
          path = 'lib/src/features/swipes/presentation/screens/client_swipe_container.dart'
          old = '''  void _goMessages() {
            ref.read(navTabProvider.notifier).set(NavTab.messages);
            GoRouter.of(context).go(AppPaths.messages);
          }
'''
          new = '''  void _goMessages() {
            final router = GoRouter.of(context);
            final rootNav = Navigator.of(context, rootNavigator: true);
            ref.read(navTabProvider.notifier).set(NavTab.messages);
            if (rootNav.canPop()) rootNav.pop();
            WidgetsBinding.instance.addPostFrameCallback((_) {
              router.go(AppPaths.messages);
            });
          }
'''
          replace_once(path, old, new, 'root swipe-deck messages navigation')

          old = '''        if (cat == 'events') {
                  Navigator.of(context).pop();
                  ref.read(navTabProvider.notifier).set(NavTab.events);
                  return;
                }
'''
          new = '''        if (cat == 'events') {
                  final router = GoRouter.of(context);
                  final rootNav = Navigator.of(context, rootNavigator: true);
                  ref.read(navTabProvider.notifier).set(NavTab.events);
                  if (rootNav.canPop()) rootNav.pop();
                  WidgetsBinding.instance.addPostFrameCallback((_) {
                    router.go(AppPaths.exploreEvents);
                  });
                  return;
                }
'''
          replace_once(path, old, new, 'exhausted deck events navigation')

          # Events teaser: full-card instant navigation, with only media buttons
          # consuming taps. Static overlays must never steal the gesture.
          path = 'lib/src/features/dashboard/presentation/widgets/events_teaser_card_v2.dart'
          replace_once(
              path,
              '''            Positioned(
              top: 0,
              left: 0,
              right: 52,
              bottom: 52,
              child: GestureDetector(
''',
              '''            Positioned.fill(
              child: GestureDetector(
''',
              'events full-card tap target')

          replace_once(
              path,
              '''            Positioned(
              left: 14,
              top: 13,
              child: Container(
                padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 6),
                decoration: BoxDecoration(
                  color: Colors.black.withAlpha(125),
                  borderRadius: BorderRadius.circular(999),
                  border: Border.all(color: Colors.white.withAlpha(42)),
                ),
                child: Text(
                  _videoPreviewEnabled ? 'EVENTS  •  LIVE' : 'EVENTS  •  PAUSED',
                  style: GoogleFonts.plusJakartaSans(
                    color: Colors.white,
                    fontSize: 9,
                    fontWeight: FontWeight.w900,
                    letterSpacing: 1.1,
                  ),
                ),
              ),
            ),
''',
              '''            IgnorePointer(
              child: Positioned(
                left: 14,
                top: 13,
                child: Container(
                  padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 6),
                  decoration: BoxDecoration(
                    color: Colors.black.withAlpha(125),
                    borderRadius: BorderRadius.circular(999),
                    border: Border.all(color: Colors.white.withAlpha(42)),
                  ),
                  child: Text(
                    _videoPreviewEnabled ? 'EVENTS  •  LIVE' : 'EVENTS  •  PAUSED',
                    style: GoogleFonts.plusJakartaSans(
                      color: Colors.white,
                      fontSize: 9,
                      fontWeight: FontWeight.w900,
                      letterSpacing: 1.1,
                    ),
                  ),
                ),
              ),
            ),
''',
              'events live label pointer transparency')

          replace_once(
              path,
              '''            Positioned(
              left: 15,
              right: 15,
              bottom: 15,
              child: Row(
                crossAxisAlignment: CrossAxisAlignment.end,
                children: [
                  Expanded(
                    child: Column(
                      crossAxisAlignment: CrossAxisAlignment.start,
                      mainAxisSize: MainAxisSize.min,
                      children: [
                        Text(
                          event?.title.trim().isNotEmpty == true
                              ? event!.title.toUpperCase()
                              : 'WHAT\\'S HAPPENING',
                          maxLines: 2,
                          overflow: TextOverflow.ellipsis,
                          style: GoogleFonts.plusJakartaSans(
                            color: Colors.white,
                            fontSize: 16,
                            height: 1.02,
                            fontWeight: FontWeight.w900,
                            letterSpacing: -.35,
                          ),
                        ),
                        const SizedBox(height: 4),
                        Text(
                          _videoPreviewEnabled
                              ? (videos.length > 1
                                  ? 'Live event stream · swipe left or right'
                                  : 'Tap to explore events')
                              : 'Video preview off · tap camera to resume',
                          style: GoogleFonts.plusJakartaSans(
                            color: Colors.white,
                            fontSize: 9.5,
                            fontWeight: FontWeight.w700,
                          ),
                        ),
                      ],
                    ),
                  ),
                  const SizedBox(width: 42),
                ],
              ),
            ),
''',
              '''            IgnorePointer(
              child: Positioned(
                left: 15,
                right: 15,
                bottom: 15,
                child: Row(
                  crossAxisAlignment: CrossAxisAlignment.end,
                  children: [
                    Expanded(
                      child: Column(
                        crossAxisAlignment: CrossAxisAlignment.start,
                        mainAxisSize: MainAxisSize.min,
                        children: [
                          Text(
                            event?.title.trim().isNotEmpty == true
                                ? event!.title.toUpperCase()
                                : 'WHAT\\'S HAPPENING',
                            maxLines: 2,
                            overflow: TextOverflow.ellipsis,
                            style: GoogleFonts.plusJakartaSans(
                              color: Colors.white,
                              fontSize: 16,
                              height: 1.02,
                              fontWeight: FontWeight.w900,
                              letterSpacing: -.35,
                            ),
                          ),
                          const SizedBox(height: 4),
                          Text(
                            _videoPreviewEnabled
                                ? (videos.length > 1
                                    ? 'Live event stream · swipe left or right'
                                    : 'Tap to explore events')
                                : 'Video preview off · tap play to resume',
                            style: GoogleFonts.plusJakartaSans(
                              color: Colors.white,
                              fontSize: 9.5,
                              fontWeight: FontWeight.w700,
                            ),
                          ),
                        ],
                      ),
                    ),
                    const SizedBox(width: 42),
                  ],
                ),
              ),
            ),
''',
              'events title pointer transparency')

          p = Path(path)
          text = p.read_text()
          if text.count('Colors.black.withAlpha(108)') != 2:
              raise SystemExit('events control background: expected 2 matches')
          text = text.replace('Colors.black.withAlpha(108)', 'Colors.black.withAlpha(132)')
          if text.count('Colors.white.withAlpha(50)') != 2:
              raise SystemExit('events control border: expected 2 matches')
          text = text.replace('Colors.white.withAlpha(50)', 'Colors.white.withAlpha(48)')
          text = text.replace('const SizedBox(height: 4)', 'const SizedBox(height: 6)', 1)
          old_icon = '''                        _videoPreviewEnabled
                            ? Icons.videocam_rounded
                            : Icons.videocam_off_rounded,
'''
          new_icon = '''                        _videoPreviewEnabled
                            ? Icons.pause_rounded
                            : Icons.play_arrow_rounded,
'''
          if old_icon not in text:
              raise SystemExit('events play/pause icon pattern missing')
          text = text.replace(old_icon, new_icon, 1)
          p.write_text(text)
          print('patched: events media controls visual parity')

          # Decorative +new badges must not block the underlying quick-filter tap.
          path = 'lib/src/features/dashboard/presentation/screens/bento_dashboard_screen.dart'
          replace_once(
              path,
              '''    final badgeWidget = unreadCount > 0 
      ? Positioned(
          top: 10,
          right: 10,
          child: _CategoryBadge(count: unreadCount),
        )
      : const SizedBox.shrink();
''',
              '''    final badgeWidget = unreadCount > 0
      ? IgnorePointer(
          child: Positioned(
            top: 10,
            right: 10,
            child: _CategoryBadge(count: unreadCount),
          ),
        )
      : const SizedBox.shrink();
''',
              'quick-filter badge pointer transparency')

          Path('.github/workflows/one-shot-fix-dashboard-controls.yml').unlink()
          PY

      - name: Commit fixed source to main
        shell: bash
        run: |
          git config user.name "Swipess Fix Bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "fix(dashboard): make media controls and deck exits deterministic"
          git push origin HEAD:main
