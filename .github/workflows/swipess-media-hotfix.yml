name: SWIPESS media hotfix

on:
  push:
    paths:
      - '.github/workflows/swipess-media-hotfix.yml'

permissions:
  contents: write

jobs:
  test-runner:
    runs-on: ubuntu-latest
    steps:
      - run: echo "runner-ok"
