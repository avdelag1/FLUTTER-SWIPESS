name: Fix dashboard microphone polish

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/fix-dashboard-voice-polish.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply microphone patch
        run: python3 .github/scripts/fix_dashboard_voice_polish.py

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Format touched Dart files
        run: dart format lib/src/core/widgets/glow_search_bar.dart lib/src/features/ai/presentation/services/live_voice_input.dart

      - name: Dependencies
        run: flutter pub get

      - name: Analyze touched Dart files
        run: flutter analyze --no-fatal-infos --no-fatal-warnings lib/src/core/widgets/glow_search_bar.dart lib/src/features/ai/presentation/services/live_voice_input.dart

      - name: Commit microphone fix
        run: |
          git config user.name swipess-hotfix-bot
          git config user.email actions@users.noreply.github.com
          git add lib/src/core/widgets/glow_search_bar.dart lib/src/features/ai/presentation/services/live_voice_input.dart
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "fix(voice): polish dashboard mic and prevent premature send"
          git push origin HEAD:main
